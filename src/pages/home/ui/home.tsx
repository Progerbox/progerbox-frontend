import styles from "./home.module.scss";
import { Header } from "../../../shared/components/header";

export const HomePage = () => {
  // return <div className={styles.test}>Hello, Word!</div>;
  return <Header name={"Progerbox"} />;
};
